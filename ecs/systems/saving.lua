local system = {}

-- @TODO: have an entity with a component that holds settings for this system.
--        maybe a singleton settings entity?

local filter = function()
end

local function action()
end

return {
    author  = "HuwT",
    name    = "saving",
    filters = {},
    events  = {},
}